---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-sidecar
spec:
  selector:
    app: {{ .Release.Name }}-sidecar
  ports:
    - port: 8000
      targetPort: http
      name: http
